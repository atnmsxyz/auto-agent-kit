{
  "mcpServers": {
    "auto": {
      "command": "npx",
      "args": ["-y", "auto-mcp"],
      "env": {
        "AUTO_API_KEY": "atk_REPLACE_ME",
        "AUTO_MCP_SURFACE": "research"
      }
    }
  }
}
