{
  "mcpServers": {
    "auto": {
      "command": "npx",
      "args": ["-y", "auto-mcp"],
      "env": {
        "AUTO_API_KEY": "${AUTO_API_KEY}",
        "AUTO_MCP_SURFACE": "research"
      }
    }
  }
}
