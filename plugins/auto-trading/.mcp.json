{
  "mcpServers": {
    "auto": {
      "command": "npx",
      "args": ["-y", "@atnms/auto-mcp"],
      "env": {
        "AUTO_API_KEY": "${AUTO_API_KEY}",
        "AUTO_MCP_SURFACE": "trading"
      }
    }
  }
}
